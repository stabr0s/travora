# Copy this file to .env.local for local development.
# Use values from your Supabase project settings or Connect dialog.
# Never commit .env.local or real secret values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Never expose a service_role key, JWT secret, database password,
# or any other Supabase secret key in the browser.
